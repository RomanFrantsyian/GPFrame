[package]
name = "rules"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
term = { path = "../term" }
harness = { path = "../harness" }
egg = "0.9"
# z3  = "0.12"          # R2: O1 discharge behind rules::smt::SmtBackend seam
