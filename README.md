# GPFrame
A Rust engine that extracts pure numeric functions into a verified term language, then refactors, tests, debugs, and JIT-compiles them — with every result exiting only through proof (SMT/e-graph) or a quantified statistical gate. No claim ships without a certificate stating exactly what holds, where, and with what confidence.
